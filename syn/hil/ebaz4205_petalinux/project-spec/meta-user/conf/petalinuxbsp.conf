#User Configuration

#OE_TERMINAL = "tmux"
IMAGE_BOOT_FILES:zynq = "BOOT.BIN,boot.scr,image.ub"

# Ambiente sandboxed sem suporte a user namespaces — ver
# meta-user/classes/sandbox-no-netns.bbclass para o motivo.
INHERIT += "sandbox-no-netns"
