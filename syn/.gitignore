
# Ignore everything
*
# Keep everything bellow
# Allows to include files in subdirectories without having to include every
# parent folder as well.
!*/
# Project Files
!/*.xpr
!/*.xdc
!/.gitignore
# Memory creation script
!/mem.tcl
# IPs
!/mvw*-cce-fpga.srcs/sources_1/ip/**
!/mvw*-cce-fpga.ip_user_files/**
# Binaries
!/*.bit
!/*.bin